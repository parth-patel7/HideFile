                        HideFile

Purpose: To hide files in user space by modifying library function that reads directory contents.

Algorithm: Program will interpose as readdir so that it will hide a secret file whose name is set by an environment variable. Using LD_PRELOAD and setting environment variable to HIDDEN

Testing: Use the makefile to test the program, "make test"
